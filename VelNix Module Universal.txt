local GlobalSetup = {}

function GlobalSetup.setup()
    _G.output_destroyer = function(...)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "[ VelNix Hub - Skiddlers Detected ]",
            Text = "So You Thought?"
        })
    end

    _G.print = _G.output_destroyer
    _G.warn = _G.output_destroyer
    _G.info = _G.output_destroyer

    local original_setclipboard = setclipboard
    _G.setclipboard = function(content)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "[ VelNix Hub - Skiddlers Detected ]",
            Text = "So You Thought?"
        })
    end

    print = _G.print
    warn = _G.warn
    info = _G.info
    setclipboard = _G.setclipboard

    print("Global function overrides applied.")
end

return GlobalSetup